---
import { SITE } from '#config';

interface Props {
  date: Date;
}

const { date } = Astro.props;

const formattedDate = date.toLocaleDateString(`${SITE.lang}-${SITE.region}`, {
  dateStyle: 'long',
  timeZone: 'Asia/Tokyo',
});
---

<time datetime={date.toISOString()}>{formattedDate}</time>
